#include <stdio.h>
#include <string.h>

int main(void)
{
  char a[16];
  scanf("%s", a);
  int count = 0;
  char b[24]

  for (int i=0; i<strlen(a); i++)
  {
    count = count + a[i]
  }


  printf("%d", strlen(a) + count);

  return 0;
}
