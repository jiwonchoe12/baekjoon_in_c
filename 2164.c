#include <stdio.h>

int main(void)
{
  int n;
  int card[250000];
  scanf("%d", &n);
  if (n==1)
    printf("%d", n);
  else
  {
    if (n%2==0) //짝수
    {
      for (int i=2; i<n; i=i+2)
      {

      }
    }
    else //홀수
    {
      for (int i=2; i<n; i=i+2)
      {

      }
    }
  }

  return 0;
}
